services:
  app:
    build: .
    environment:
      - PORT=8080
      - NODE_ENV=production
      - WORKDIR=/app
      - DMR_URL
      - VCR_URL
    # network_mode: host
    ports:
      - 8080:8080
    restart: unless-stopped
